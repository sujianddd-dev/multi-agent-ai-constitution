# 项目上下文（CONTEXT）— 所有 Agent 必读

> 本文件回答："我们在做什么、做到哪一步了、谁负责什么、有哪些红线。"
> 任何 Agent 开始工作前必须读取本文件。

---

## 📌 项目目标

- 构建一个 **XXX** 系统
- 核心特性是 **YYY**
- **不处理** ZZZ 场景（明确不做的事，和做一样重要）
  - 例：不处理非目标市场的业务
  - 例：**不收集用户的隐私信息**（隐私红线，如适用）

---

## 🎯 当前阶段

**Phase 1：项目初始化**

- ✅ 完成：（无）
- 🔄 进行中：建立共享知识库（本宪法）
- ⏳ 待做：填写本文件占位符、任命主 Agent、拆分首批任务

> 阶段状态由人类/主 Agent 更新；Agent 按当前阶段认领任务。

---

## 🤖 Agent 分工

| Agent | 职责（建议分工，调整须记录决策日志） |
|---|---|
| Agent A | 例：代码质量审查、优化 |
| Agent B | 例：架构设计、复杂逻辑 |
| Agent C | 例：测试用例、文档 |

> 用你实际的工具链替换（如 zcode / Codex / Claude Code / Kimi Work）。

---

## ⚙️ 技术栈

> 以实际项目为准，示例：

- Python 3.11
- FastAPI
- PostgreSQL
- Docker

> Agent **不得**擅自引入技术栈之外的依赖（见下方禁止事项）。

---

## 🚫 禁止事项（红线）

- ❌ 不修改数据库 schema
- ❌ 不改变 public API 签名
- ❌ 不引入新的依赖（未经批准）
- ❌ 不直接修改 main 分支
- ❌ 不删除其他 Agent 的代码（需先讨论）
- ❌ 不修改已批准的架构

---

## ✅ 审核清单

**任何提交（PR）之前必须：**

- [ ] 通过所有测试
- [ ] 更新相关文档
- [ ] 遵循代码规范（CODING_STANDARDS.md）
- [ ] 不违反 API 合同（API_CONTRACT.md）
- [ ] 在 PR 描述中引用本次任务依据的本文件版本

---

## ✍️ 本文件维护规则

- 由**人类或主 Agent** 修改（权限依据：README「治理结构」审批权矩阵）
- 普通 Agent 发现上下文过期时，**提 PR 建议修改**，不得直接改
